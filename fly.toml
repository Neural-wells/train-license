# fly.toml app configuration file generated for train-license on 2026-08-14T06:57:00Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'train-license'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  APP_URL = 'https://train-license.fly.dev'
  DB_PATH = '/data/train.db'

[[mounts]]
  source = 'tl_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'suspend'
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
